#include"main.h"
/**
 * mul - function multiplies two integers.
 *
 * @a: checks function input.
 * @b: checks function input.
 *
 * Return: Always 0 (Success).
*/
int mul(int a, int b)
{
	int prod = 0;

	prod = a * b;

	return (prod);
}
